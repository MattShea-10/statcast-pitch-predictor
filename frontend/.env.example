# In production (e.g. Vercel), set this to your deployed backend's base URL,
# no trailing slash. Leave unset for local dev (Vite proxies /api instead).
VITE_API_URL=https://statcast-pitch-predictor-api.onrender.com
